#include "monty.h"

/**
 * _nop - function that doesn't do anything
 * @stack:
 * @line_number:
 * Return:
 */
void _nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
